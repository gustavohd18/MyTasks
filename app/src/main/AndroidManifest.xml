<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mytasks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.task.CreateTaskActivity"
            android:theme="@style/AppThemeNoBar"
            >

        </activity>
        <activity android:name=".views.login.NewPasswordActivity" />
        <activity
            android:name=".views.login.ForgetActivity"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".views.login.CreateActivity"
            android:theme="@style/AppThemeNoBar" />
        <activity android:name=".views.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.MainActivity"
            android:theme="@style/AppThemeNoBar"
            >
        </activity>
    </application>

</manifest>